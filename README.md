# Shiny_bakalarka
n interactive web application built with **Shiny for Python** for visualizing and evaluating outputs from the **multiscale Hydrologic Model (mHM)** and **multiscale Routing Model (mRM)**.
